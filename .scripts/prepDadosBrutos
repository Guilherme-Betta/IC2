import pandas as pd

caminhoDaPasta = input("Insira o caminho da Pasta: ") # Gera uma prompt para o usuário inserir o diretório do arquivo que será alterado
caminhoDaPasta = caminhoDaPasta.strip('"').replace('\\', '/') # Altera o texto inserido para que o python consiga ler

dadosBrutos = pd.ExcelFile(caminhoDaPasta) 

dadosBrutos = pd.read_excel(caminhoDaPasta)

print(dadosBrutos)