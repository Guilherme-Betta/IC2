import pandas as pd
import missingno as msno
import matplotlib.pyplot as plt

arquivo_entrada = "C:\\_Master\\Google Drive\\Faculdade\\IC2\\Dados\\dadosBrutos.xlsx" # Gera uma prompt para o usuário inserir o diretório do arquivo que será alterado
arquivo_entrada = arquivo_entrada.strip('"').replace('\\', '/') # Altera o texto inserido para que o python consiga ler

dados = pd.ExcelFile(arquivo_entrada)
dados = pd.read_excel(arquivo_entrada)

# contagem = dados['COR'].value_counts().plot(kind='kde')

variaveis = [colunas for colunas in dados.columns if colunas not in 'Estatistica' + 'Data']

contador_NaN = dict.fromkeys(variaveis, 0)  # Cria uma biblioteca para guardar o número de dados faltantes em cada variavel
contador_gen = dict.fromkeys(variaveis, 0)  # Cria uma biblioteca para guardar o número de dados não numéricos em cada variavel

# print(contador_gen['COR'], contador_NaN['COR'])

print(dados.loc[dados['COR'] == 'NaN'].unique())
"""
for col in variaveis:
    dados.loc[dados[col] == 'NaN'].value_counts"""